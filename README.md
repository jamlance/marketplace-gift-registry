# marketplace-gift-registry

Deploy repo for the **gift-registry** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs gift-registry --push`.

Coolify: base_directory `/`, dockerfile `/apps/gift-registry/Dockerfile`.
